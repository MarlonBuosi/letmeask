# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request

jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install --frozen-lockfile && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_LETMEASK_874FF }}'
          projectId: letmeask-874ff
        env:
          REACT_APP_apikey: '${{ secrets.REACT_APP_API_KEY}}'
          REACT_APP_authdomain: '${{ secrets.REACT_APP_AUTH_DOMAIN}}'
          REACT_APP_databaseurl: '${{ secrets.REACT_APP_DATABASE_URL}}'
          REACT_APP_projectid: '${{ secrets.REACT_APP_PROJECT_ID}}'
          REACT_APP_storagebucket: '${{ secrets.REACT_APP_STORAGE_BUCKET}}'
          REACT_APP_messaginsenderid: '${{ secrets.REACT_APP_MESSAGING_SENDER_ID}}'
          REACT_APP_appid: '${{ secrets.REACT_APP_APP_ID}}'
